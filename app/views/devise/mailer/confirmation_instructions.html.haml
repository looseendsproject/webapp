%p.headline Please confirm your email address

%p Hello, #{@resource.name}! You must confirm your email before proceeding.  Please click the link below:

%p= link_to 'Confirm My Email', confirmation_url(@resource, confirmation_token: @token), class: 'email-button'
