.card.flex-fill
  %h5.card-header.mt-0 Project Details
  .card-body
    %table
      %tbody
        = render partial: 'show_row', locals: { title: 'Craft Type', value: @project.craft_type }
        = render partial: 'show_row', locals: { title: 'Description', value: @project.description }
        = render partial: 'show_row', locals: { title: 'Recipient', value:  @project.recipient_name }
        = render partial: 'show_row', locals: { title: 'Phone Number', value: @project.phone_number, copy_paste: true }
        - if @project.missing_address_information?
          = render partial: 'show_row', locals: { title: 'Address', value: false }
        - else
          = render partial: 'show_row', locals: { title: 'Address', value: @project.full_address, link: link_to(fa_icon('map'), "https://maps.google.com/?q=#{@project.full_address}", target: "_blank") }
        = render partial: 'show_row', locals: { title: 'Phone Number', value: @project.phone_number }
        = render partial: 'show_row', locals: { title: 'More Details', value: @project.more_details }