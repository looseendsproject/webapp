.col
  - if !@project
    = link_to assignment.project.name, [ :manage, assignment.project ]
  = link_to 'assigned', [ :manage, assignment ]
  - if !@finisher
    to
    = link_to assignment.finisher.user.name, [ :manage, assignment.finisher ]
  = l assignment.created_at, format: :short
