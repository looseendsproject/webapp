%h1
  = @user.name
  = link_to 'Edit User', [:edit, :admin, @user], class: 'btn btn-outline-secondary float-end'

.row
  .col
    Since: #{l @user.created_at, format: :long}
  .col
    Email: #{@user.email}
%p
  Role: #{@user.role} #{@user.is_project_manager ? '(Matcher)' : ''}
