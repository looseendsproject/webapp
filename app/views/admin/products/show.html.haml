%h1
  = @product.name
  = link_to 'Edit', [:edit, :admin, @product], class: 'btn btn-outline-secondary float-end'
= link_to pluralize( @product.volunteers.count, 'volunteer'), [:manage, :volunteers, { :product_id => @product.id }]
