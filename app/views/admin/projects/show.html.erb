<%= render 'title', title: @project.name %>
<%= link_to 'Edit', [:edit, @project] %>
<p><%= @project.description %></p>
<% @project.images.each do |image| %>
  <%= image_tag image.representation( resize_to_limit: [100, 100]) %>
<% end %>