%h1
  = @skill.name
  = link_to 'Edit', [:edit, :admin, @skill], class: 'btn btn-outline-secondary float-end'
= link_to pluralize( @skill.finishers.count, 'finisher'), [:manage, :finishers, { :skill_id => @skill.id }]
